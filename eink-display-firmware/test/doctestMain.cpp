// doctest is header-only: exactly one translation unit must supply its
// implementation and main(). Everything else just includes <doctest.h>.

#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include <doctest.h>
